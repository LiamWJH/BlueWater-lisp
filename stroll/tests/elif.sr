(if (< 2 1)
    (print "2 is bigger than 1")
    
    (elif (== 3 5)
        (print "3 is smaller than 5")
    )

    (elif (!= "5" "5")
        (print "five is five")
    )

    (else
        (print "WTF?!?!?")
    )
)
